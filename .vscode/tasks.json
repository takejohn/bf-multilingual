{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "c_build",
            "type": "shell",
            "command": "make",
            "args": ["build"],
            "options": {
                "cwd": "${workspaceFolder}/implementations/c/"
            }
        }
    ]
}
